<!DOCTYPE html>
<html lang="en">

<head>
    <title>Gator Tot's BS Registration</title>
    <%- include('../shared/head') %>
</head>

<body>
    <div id="container">
            <h1>Register</h1>

            <!-- Display error message if it exists -->
            <% if (locals.error && error) { %>
                <div class="error-message" style="color: rgb(255, 0, 0)">
                    <%= error %>
                </div>
                <% } %>

                    <form action="/auth/register" method="POST">
                        <div>
                            <label for="username">Username:</label>
                            <input type="text" id="username" name="username" required placeholder="Username">
                        </div>
                        <div>
                            <label for="password">Password:</label>
                            <input type="password" id="password" name="password" required placeholder="Password">
                        </div>
                        <button type="submit" class="button-link" display: flex justify-content: flex-end>Register</button>
                    </form>
                    <div>
                        <br>
                        Already have an account? Please <a href="/auth/login" class="button-link">Login</a>
                    </div>
    </div>
</body>

</html>